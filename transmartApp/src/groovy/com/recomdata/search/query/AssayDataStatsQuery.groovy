package com.recomdata.search.query

import groovy.transform.CompileStatic

/**
 * @author mmcduffie
 */
@CompileStatic
class AssayDataStatsQuery extends AssayAnalysisDataQuery {
    // just in case we need to create some special rules
}
